// src/ui/hub/CrewCard.jsx — a slot: the crew card (create/join/rename/leave) is built in a later task.
// HubSheet renders this only when CLOUD is true.
export default function CrewCard(){
  return null;
}
