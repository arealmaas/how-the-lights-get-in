// src/ui/hub/CrewSection.jsx — a slot: the crew calendar and reading list (Phase 2) are built in a later
// task. HubSheet renders this only when CLOUD is true.
export default function CrewSection(){
  return null;
}
