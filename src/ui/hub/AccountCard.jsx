// src/ui/hub/AccountCard.jsx — a slot: the account card (sign-in, name, sync status, delete account) is
// built in a later task. HubSheet renders this only when CLOUD is true.
export default function AccountCard(){
  return null;
}
